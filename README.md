# Cross Domain Molecular Transfer Learning

# Installation

This repository uses uv for dependency management, to install `uv` use:
```bash
pip install uv
```

To install dependencies use:
```bash
uv sync
```

# Data
To download the datasets used in this repository, run:
```bash
./download_data.sh
```
